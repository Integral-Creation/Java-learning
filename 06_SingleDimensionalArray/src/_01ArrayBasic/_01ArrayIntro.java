package _01ArrayBasic;

public class _01ArrayIntro {
    public static void main(String[] args) {
        /*
        An array in Java is a fixed-size collection of elements of the same data type stored in contiguous memory.
        It allows you to store multiple values in a single variable instead of creating many separate variables.

        a single array variable can reference a large collection of data.

            once an array is created its size is fixed.
            an array references variable is used to access the elements in an array using index.
        */

        /*
        declaration:
            syntax:
                    elementType[] arrayRefVar;

                    elementType can be any data type such that all the element has same data type;
         */

        // array declaration
        // double[] myList;

    }
}
