public class _01Intro {
    public static void main(String[] args) {
        /*
        Object-oriented programming enables us to develop large-scale software and GUIs effectively.

        #class
            -a class defines the properties and behaviors for objects
        #objects
            -an objects represents a real world entity that can be distinctly identified.
            -an objects is an instance of a classes.
                ex- a student, desk, a button etc. can be viewed as objects.
            -an objects has a unique identity state and behavior
                + the state of an objects(properties) is represented by data fields with their current values.
                    ex- a circle has a data fields of radius
                        a rectangle has a data fields of widths and height.
                + the behavior of an objects(actions) is defined by methods.to invoke method on an objects is to ask performs an actions.
                    ex- getArea() and getPerimeter() etc.

        A java class use variables to define data fields and methods to define actions.

        #constructor

        */
    }
}
